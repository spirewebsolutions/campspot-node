import IntervalSearchService from './IntervalSearchService';
import LinearSearchService from './LinearSearchService';

/**
 * Responsible for constructing an implementation of
 * a search service given a predicate
 */
export default class SearchServiceFactory{
	static buildSearchService(interval) {
		return 
	}
}
